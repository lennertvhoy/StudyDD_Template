# Reviews

`reviews/` contains spaced repetition queues.

The queue is driven by evidence. A weak or repaired answer should create a review item. A confirmed skill should not be reviewed just because the learner touched it once.

Use `reviews/REVIEW_QUEUE.md` as the default queue unless a future add-on creates a more specialized queue file.
