# Demo fixtures

`scripts/run_demo_replay.py --json` emits
`studydd.local-alpha-demo/v1`, marks itself synthetic-only and public-safe, and
does not include learner transcript content in the JSON summary. Runtime demo
state is temporary and is not committed.
