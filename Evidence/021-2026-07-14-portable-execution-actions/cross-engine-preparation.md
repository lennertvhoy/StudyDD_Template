# Cross-engine preparation

StatePort can prepare the same action against the deterministic synthetic and
provider-neutral API fixture profiles. Pi, Codex, and OpenCode are represented
as environment-gated profiles; StudyDD state does not change when a profile is
selected.
