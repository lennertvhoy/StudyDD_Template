# Action contracts

Two typed actions are declared in `contracts/studydd.actions.yaml`: planning
the next session and inspecting due review debt. Inputs and outputs are schemas,
with explicit context, capability, budget, network, tool, mutation, validation,
degradation, and evidence fields.
