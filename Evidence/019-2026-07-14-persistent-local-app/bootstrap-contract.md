# Bootstrap contract

StudyDD declares the typed `studydd.bootstrap/v1` contract. It owns the
semantics of identity, owner, target, timezone, goal, seed mode, and valid
initial document shapes. StatePort consumes the contract without executing
arbitrary template scripts.
