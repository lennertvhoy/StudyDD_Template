# Validation

The convergence workflow commands passed locally, including StudyDD health,
manifest/origin coverage, instantiation, study-loop, compact-state,
context-pack, learning activities, next-activity decisions, source freshness,
cross-platform paths, source-check completion, Fast Drill, question quality,
and instance-layout contract tests.

The health validator reports only the existing derived-context freshness
warning; no learner content or private canary content was introduced.
