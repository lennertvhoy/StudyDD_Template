# Known limits

- Release identity remains `0.11.0` with machine-readable candidate status;
  prerelease syntax is not assumed by the existing manifest contract.
- The question-bank engine is not selected and has no runtime import/export
  engine.
- The Fast Drill legacy-setting migration is not yet wired into StatePort’s
  migration registry.
- The complete immutable 0.10 → 0.11 proof and generated-view composition are
  deferred to StatePort integration review.
