StudyDD performance policy test
===============================

Test: performance budget file exists and validates
Performance budget OK
StudyDD create-instance
=======================
1. Copying template to /tmp/studydd-perf-n7zo4yos/Study_Perf
2. Initializing Git
3. Switching to bootstrap mode
4. Recording template origin metadata
5. Running bootstrap validation
StudyDD validation
==================

Warnings:
  - Bootstrap mode: learner profile and first target are not initialized yet. Complete personalization and then switch mode to learner_instance.

All required files present.
YAML validation passed.
No forbidden mentions found.
StudyDD state looks healthy.

Bootstrap validation passed.

Next step: open the new instance in your coding agent and paste the following prompt:

# Coding Agent Start Prompt for StudyDD

You are a coding agent operating inside a StudyDD repository. StudyDD is a repo-native study brain, not a human-facing app. Your job is to operate the learning loop in plain files.

## Before You Do Anything Else

1. Verify repo path: run `pwd` and `git rev-parse --show-toplevel`. Confirm the root is the intended StudyDD directory. If not, stop.
2. Verify remote: run `git remote -v` and confirm it matches the learner's StudyDD repo.
3. Run `python3 scripts/check_studydd.py`.
4. Read `AGENTS.md`.
5. Read all files listed in `AGENTS.md` "Required First Actions".
6. Run `python3 scripts/compact_state.py` and `python3 scripts/build_context_pack.py --task start_session`.
7. Read `.studydd/context_pack.md` and the active target's `study_skills/<study_skill>/SKILL.md` (or generic fallback).

## If This Is Still The Public Template

Do not seed a learner, target, or exam unless the learner explicitly asks you to initialize this copy.

Ask only the essential setup questions:

- What should I call you?
- What do you want to learn or prepare for?
- Is there a deadline?
- What language and tutoring style should I use?
- What trusted sources should I use first?

Then initialize the happy path:

1. Update learner profile in `state/STUDY_STATE.yaml` and `state/STUDY_STATUS.md`.
2. Create the first target folder in `targets/` with `TARGET.yaml`.
3. Register trusted sources in `sources/SOURCE_INDEX.md`.
4. Build a conservative `state/SKILL_MAP.yaml`.
5. Set `NEXT_ACTIONS.md` to the first one-question tutoring action.
6. Run `python3 scripts/check_studydd.py`.

## During Study Sessions

- At session start, run or perform the equivalent of `scripts/select_next_study_action.py`. If reviews are due or overdue, recommend review first before new material.
- Ask exactly one question at a time.
- Before asking or grading, load the active target's study skill from `study_skills/<study_skill>/SKILL.md`. The study skill controls question style, grading emphasis, evidence requirements, and readiness upgrade rules.
- Use `protocols/TUTOR_PROTOCOL.md` and `protocols/QUESTION_QUALITY.md`.
- Define the answer key internally before asking. Do not reveal it.
- Grade the learner's actual answer, not the answer you expected.
- Tag mistakes using `protocols/MISTAKE_TAXONOMY.md`.
- If the answer is wrong or incomplete, ask a focused repair question before moving on.
- Record every interaction in `sessions/SESSION_LOG.md` and `state/EVIDENCE_LOG.md`.
- Update `state/SKILL_MAP.yaml` and `state/STUDY_STATE.yaml` only from evidence.
- Add weak or repaired items to `reviews/REVIEW_QUEUE.md` and `reviews/REVIEW_STATE.yaml` using `scripts/schedule_review.py`.
- Never inflate readiness.
- Preserve human overrides in evidence, session logs, and `reviews/REVIEW_OVERRIDES.md`.
- End every session with a proposed state update and one clear next action in `NEXT_ACTIONS.md`.

If the learner skips a due review, say: "Recommended by StudyDD: review first. You can override, but this is the highest-retention move." Then record the override.

## State Update Discipline

Before writing study-state changes, propose them to the learner. Wait for confirmation unless the learner has explicitly authorized automatic updates. Always explain what changed and why.

## Correction Policy

If the learner challenges your grading or you discover a mistake, stop and audit. Update the state files to reflect the correction rather than hiding the error.

## Start Now

Greet the learner briefly, summarize what you have read from the current state, and ask what they would like to study or initialize today.


Instance created at: /tmp/studydd-perf-n7zo4yos/Study_Perf


Test: context pack reports file counts and skips raw logs on fast path
StudyDD context pack built.

Task: grade_answer
Mode: fast_path
Files included: 12
Files skipped: 4
Raw log files loaded: 0
Cache used: True

Included:
- state/STUDY_STATE.yaml: canonical state or manifest
- state/SKILL_MAP.yaml: canonical state or manifest
- reviews/REVIEW_STATE.yaml: canonical state or manifest
- state/STUDYDD_MODE.yaml: canonical state or manifest
- state/STUDYDD_TEMPLATE_VERSION.yaml: canonical state or manifest
- state/STATE_MANIFEST.yaml: canonical state or manifest
- state/PERFORMANCE_BUDGET.yaml: canonical state or manifest
- targets/perf-target/TARGET.yaml: active target metadata
- state/CURRENT_CONTEXT.md: derived summary or index
- state/EVIDENCE_INDEX.yaml: derived summary or index
- sessions/SESSION_SUMMARIES.md: derived summary or index
- NEXT_ACTIONS.md: derived summary or index

Skipped:
- study_skills/perf-search-basics/SKILL.md: declared study skill file missing
- state/EVIDENCE_LOG.md: raw audit log; indexed evidence is enough
- sessions/SESSION_LOG.md: raw audit log; session summaries are enough
- reviews/REVIEW_OVERRIDES.md: raw audit log; not needed for this task

Output: .studydd/context_pack.md

Context pack fast path OK

Test: compact_state --check-stale does not rewrite files unnecessarily
State is up to date. No compaction needed.

Stale check OK

Test: demo replay mentions fast path and minimal loading
Building StudyDD context pack instead of loading every file...
  StudyDD context pack built.
  
  Task: start_session
  Mode: session_boundary
  Files included: 13
  Files skipped: 3
  Raw log files loaded: 0
  Cache used: False
  
  Included:
  - state/STUDY_STATE.yaml: canonical state or manifest
  - state/SKILL_MAP.yaml: canonical state or manifest
StudyDD demo replay
===================

1. Created learner instance from template.
2. Initialized learner profile: Demo Learner.
3. Initialized target: AI Search Fundamentals Demo.
4. Active study skill: it_certification.
5. Built a context pack instead of loading every file.
6. StudyDD uses the fast path during ordinary tutoring turns.
7. Only relevant state is loaded.
8. Only touched files are updated.
9. Full validation runs at session boundary.
10. Agent asked one question: Q-DEMO-001.
11. Learner answered: distinguished keyword and vector search, no scenario.
12. Agent graded honestly: partial.
13. Evidence recorded: ev_demo_001.
14. Review scheduled: rev_demo-search-basics_20260624_100000 due in 1 day.
15. Selector before due: new material is allowed.
    StudyDD recommendation: new material is allowed.
16. Selector when review is due: review first.
     StudyDD recommendation: review first.
17. Learner override recorded with reason.
18. Validation passed.

The repo now contains evidence, a spaced-repetition review, an override log,
a compact context pack, and a clear next action. Raw logs remain available
for audit, but the agent loads only the relevant context by default.
Run `python3 scripts/check_studydd.py` in the instance to verify repo health.

Demo replay mentions fast path OK

Test: full validator still passes
StudyDD validation
==================

All required files present.
YAML validation passed.
No forbidden mentions found.
StudyDD state looks healthy.

Performance policy test passed.
