# StudyDD Template Engineering Backlog

## Next lifecycle slice

1. Add Git-backed immutable source commit/tree resolution and a lock-backed
   controlled upgrade plan/apply path in StatePort.
2. Reconcile the clean extraction order for PR #4, PR #5, and the WIP handoff
   without treating any open branch as canonical template content.

The learning queue remains in `NEXT_ACTIONS.md`. This file contains template
engineering work only and does not represent learner state.
