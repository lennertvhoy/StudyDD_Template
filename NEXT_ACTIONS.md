# NEXT_ACTIONS — Active Queue

> Generic learner-instance seed. Template engineering work is tracked in
> `TEMPLATE_BACKLOG.md`, never in learner next-action state.

## Current next action

1. Create a separate learner instance with `scripts/create_instance.py`, then initialize its learner profile and first target there.

## Pending actions

- Build a conservative skill map from trusted sources after initialization.
- Start one focused retrieval question after the target is active.
